# Tool names available to the agent under test, one per line.
#
# A placeholder input. docs/card-format.md says this vocabulary comes from an introspected
# source — the tool registry, MCP schemas, the agent roster — and none of those exist until
# the agent step lands (#9). Until then a card's tools are stated here so `unknown-tool`
# can run at all, and the file is a lint input rather than a spec surface.
#
# tau-bench airline domain.
book_reservation
calculate
cancel_reservation
get_reservation_details
get_user_details
list_all_airports
search_direct_flight
search_onestop_flight
send_certificate
think
transfer_to_human_agents
update_reservation_baggages
update_reservation_flights
update_reservation_passengers
